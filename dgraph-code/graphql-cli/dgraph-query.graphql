query {
  test(func: has(TechAsset.name)){
	TechAsset.name
  TechAsset.communication_links{
			CommunicationLink.protocol
      CommunicationLink.tech_asset_to{
				TechAsset.technology
    }
  }
  }
}

query {
  test(func: has(TechAsset.name)) {
    TechAsset.name
    TechAsset.trust_boundary{
    TrustBoundary.name
      TrustBoundary.nested_trust_boundaries {
        TrustBoundary.name
      }
    }
  }
}